
# 1. 암호화와 복호화 

## 평문 
암호화가 되지 않은 원본 데이터

## 암호화
평문의 의미를 알 수 없는 암호문으로 변환하는 과정, 데이터 **기밀성**을 보장  

```
평문 + 암호화 알고리즘 + 암호키 -> 암호문 
```

## 복호화 
암호문을 원래의 평문으로 복원하는 과정, 복호키 유출 시 복호화가 가능하여 암호키 관리가 중요 

```
암호문 + 복호화 알고리즘 + 복호키 -> 평문 
```

### 암호키 
암호화와 복호화 과정에 사용하는 특정 비트열 
동일한 알고리즘을 사용하더라도 키가 다르면 서로 다른 암호문이 됨


# 2. 암호학의 구성

- 암호기술
  데이터를 안전하게 암호화하는 기술 
  
- 암호분석
  키 없이 암호문을 해독하거나 취약점을 분석하는 기술 


# 3. 고전 암호 

### 시저 암호 
### 단일치환 암호 
### 다중치환 암호 



# 4. 암호화 단위에 따른 분류 

### 블록 암호 
데이터를 고정된 크기의 블록으로 나누어 암호화 

- DES
- 3DES
- AES

### 스트림 암호 
데이터를 비트 또는 바이트 단위로 연속적으로 암호화 

- 실시간 데이터 처리에 적합 
- 블록 단위 패딩 불필요 
- 동일한 키 스트림을 재사용 -> 보안 문제 

| 구분      | 블록 암호             | 스트림 암호       |
| ------- | ----------------- | ------------ |
| 처리 단위   | 고정 크기 블록          | 비트 또는 바이트    |
| 대표 알고리즘 | DES, 3DES, AES    | RC4 등        |
| 적합한 데이터 | 파일, 데이터베이스 저장 데이터 | 실시간 통신 데이터   |
| 주의점     | 운영모드와 패딩          | 키 스트림 재사용 금지 |


# 5. 대칭키 암호 

암호화키와 복호화키가 동일한 암호화 방식 

### 장점 
- 암호화와 복호화 속도가 빠르다.
- 대용량 데이터 암호화에 적합하다.
- 비대칭키 암호보다 비교적 적은 연산 비용이 필요하다.
- 데이터베이스, 파일, 통신 데이터 암호화에 널리 사용된다.

### 단점 
- 송신자, 수신자가 사전에 비밀키를 공유해야 한다.
- 키 전달 과정에서 유출 가능성이 있다.
- 사용자 수가 증가하면 관리해야 하는 키가 급증한다.
- 키가 유출되면 개노답이다.


### 대칭키 개수 계산 

```
n(n-1)/2
```




# 6. DES

DES : Data Encryption Standard 
과거에 널리 사용되던 블록 암호 알고리즘, 64비트 블록 암호 알고리즘, 키 길이 56비트 

|구분|크기|
|---|---|
|블록 크기|64비트|
|입력 키 크기|64비트|
|실제 암호화 키 길이|56비트|
|패리티 비트|8비트|


# 7. 3DES

3DES/ Triple-DES : DES의 짧은 키 보안 문제를 해결하기 위해 DES 연산을 세 번 수행한다.

```
EDE 방식 
DES 암호화 -> DES 복호화 -> DES 암호화 
```

- DES보다 보안성 좋음 
- 112비트 또는 168비트 수준의 키를 사용 
- 처리 속도는 느림
- 대부분 AES로 대체 


# 8. AES

AES : Advanced Encryption Standard 

DES를 대체하기 위해 선정된 표준 대칭키 블록 암호 알고리즘이다.

| 구분      | 크기              |
| ------- | --------------- |
| 블록 크기   | 128비트           |
| 지원 키 길이 | 128, 192, 256비트 |

AES는 키 길에 따라 수행하는 라운드 수가 달라짐

| 키 길이  | 라운드 수 |
| ----- | ----- |
| 128비트 | 10라운드 |
| 192비트 | 12라운드 |
| 256비트 | 14라운드 |


- DES,